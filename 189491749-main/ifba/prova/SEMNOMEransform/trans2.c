#include <stdio.h>
#define MAX 255
#define chars 2

int main(void)
{
	char atual[MAX];
	int iteracao;
    int temp;
    int qtde;
    char sequencia[255];
    scanf("%s", atual);
    scanf("%i", &iteracao);
    for (int i = 0; i < iteracao; i++)
}
